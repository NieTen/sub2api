# 工单、Telegram 社群与批量邮件

本功能沿用项目现有 Go、Vue 和 PostgreSQL 架构，复用系统 SMTP 配置，不需要增加邮件或机器人依赖。

## 功能入口

- 后台统一入口：管理员登录后点击左侧「机器人与群发」，地址为 `/admin/communications`。直接填写并保存机器人参数，使用页内导航管理邮件群发、社群/VIP、成员绑定和工单回复。
- 原有工单、邮件及社群页面地址继续可用，均要求管理员权限；普通用户不能进入后台管理页面。
- 用户工单：登录后进入「工单」，创建工单、回复、查看图片、关闭或重新打开工单。
- 管理员工单：后台进入「工单管理」，查看所有用户的工单，直接回复或变更状态。
- 工单通知：在后台配置管理员收件邮箱和 Telegram 机器人。
- 客服与社群：登录后进入「客服与社群」，打开已有客服网页，直接领取个人入群邀请，入群成功后绑定 Telegram 账号。
- 客服与社群设置：后台配置客服页面地址、Telegram 群组及机器人用户名。
- 社群成员：后台查看全部网站用户的已入群、未入群、待入群和已退群状态。
- VIP 专享群：可将当前社群设置为仅成功充值用户可见、可加入，并开启登录后的加入提示。
- 批量邮件：选择指定用户或全部启用用户，填写主题、正文和图片，生成草稿后核对实际收件人数，再确认发送。

批量邮件草稿保存收件人快照；之后新增用户不会被自动加入已有草稿。只纳入启用且持有真实邮箱的用户，自动排除项目为第三方登录生成的占位邮箱。每位收件人独立投递，不会向其他用户展示收件地址。批次页面可以查看进度和失败明细，并重试失败收件人。

## 按余额和充值记录筛选群发用户

在「批量邮件」的收件人区域，可以对指定用户或全部启用用户进一步筛选：

- 有余额：当前可用余额大于 0。
- 没余额：当前可用余额小于等于 0，包含欠费用户。
- 余额大于指定金额：严格大于，不包含等于；支持非负金额、最多 12 位整数和 8 位小数，单位与用户管理中的余额一致。
- 充值过：曾成功支付过余额充值订单；不按系统累计入账字段判断，不包含订阅购买、赠送、手动兑换及返佣入账。

余额和充值条件可以组合，例如「余额大于 100」并且「充值过」。充值使用历史支付成功记录，所以后续退款的订单仍属于曾成功支付；只有创建订单、未成功付款的用户不符合该条件。

金额通过十进制字符串传递并在数据库中按精确数值比较，避免浮点取整误差。所有筛选在生成草稿时执行，收件人和筛选条件一同保存；此后余额变化、充值或修改选择不会改变已有草稿及失败重试的收件人。没有符合条件的有效收件人时，页面提示调整筛选，不会生成空批次。

## 部署

1. 备份现有数据库，使用项目原有流程构建前端和后端。
2. 启动新版后端。程序按照既有迁移机制执行 `227_support_tickets.sql`、`228_bulk_email.sql`、`229_bulk_email_recipient_filter.sql`、`230_telegram_community.sql` 和 `231_community_direct_invites.sql`，创建工单、消息、图片、通知队列、邮件批次和社群相关表。迁移按完整文件名记录，可与上游相同编号、不同名称的迁移共存。直接邀请迁移支持领取邀请时尚未绑定 Telegram 的用户；旧迁移保持不变。升级已有邮件批次时，不会重新计算历史收件人。
3. 在原有系统设置中配置可用的 SMTP 主机、端口、认证和发件人。
4. 在「机器人与群发」中填写管理员邮箱、机器人参数并启用工单通知。SMTP 的连接和发件人仍在系统设置中配置。

图片保存在 PostgreSQL 中，适合多实例共用同一数据库；请把新增数据表纳入数据库容量规划和备份。无需开放公开图片目录。附件接口使用登录认证，普通用户只能读取自己工单中的图片。

## Telegram 配置

1. 通过 Telegram 官方的 `@BotFather` 创建机器人，获得机器人令牌。
2. 把机器人加入客服群，或者先在私聊中启动机器人。填写对应会话的数字 ID；群组 ID 通常是负数。
3. 填写允许回复工单的客服 Telegram 用户数字 ID。只有指定会话中、白名单内的用户可以回复，匿名管理员身份不适用。
4. 设置 16～256 位的回调密钥，仅使用英文字母、数字、下划线和连字符。保存后页面只显示密钥是否已配置，不回显内容；编辑时留空表示保留原值。
5. 使用网站可从公网访问的 HTTPS 地址注册回调：`https://你的域名/api/v1/support/telegram/webhook`。页面保存配置不会自动调用 Telegram 注册接口。

以下 PowerShell 7 示例从输入读取令牌和密钥，不把它们写入脚本或命令历史。输入的密钥必须与后台设置一致。执行注册会改变该机器人的回调地址，一个机器人只支持一个回调地址，建议为本站使用独立机器人。

```powershell
$ticketBotToken = Read-Host '请输入机器人令牌' -MaskInput
$ticketWebhookSecret = Read-Host '请输入后台保存的回调密钥' -MaskInput
$ticketWebhookUrl = Read-Host '请输入完整 HTTPS 回调地址'
$ticketWebhookPayload = @{
    url = $ticketWebhookUrl
    secret_token = $ticketWebhookSecret
    allowed_updates = @('message', 'chat_join_request', 'chat_member')
} | ConvertTo-Json
try {
    Invoke-RestMethod -Method Post `
        -Uri "https://api.telegram.org/bot$ticketBotToken/setWebhook" `
        -ContentType 'application/json' `
        -Body $ticketWebhookPayload
} finally {
    Remove-Variable ticketBotToken, ticketWebhookSecret, ticketWebhookPayload
}
```

注册接口说明见 [Telegram 官方机器人接口文档](https://core.telegram.org/bots/api#setwebhook)。代理和反向代理必须保留 `X-Telegram-Bot-Api-Secret-Token` 请求头。

收到工单通知后，使用 Telegram 的「回复」操作引用机器人发送的通知或图片，输入文字或发送图片，即可同步为网页中的客服回复。系统根据已保存的会话、机器人消息和工单映射定位工单，不接受单独输入工单编号来回复。网页回复也会写入同一条消息时间线。

## 客服页面和专属入群邀请

用户页面地址为 `/community`，后台设置地址为 `/admin/community/settings`。在「客服页面地址」填写已有客服页面的完整 HTTP 或 HTTPS 地址；它独立于 Telegram 社群开关，关闭社群后仍可提供客服入口。工单入口继续保留。

社群配置复用「工单通知」中保存的机器人令牌和回调密钥，但启用状态独立。可以只保存机器人凭证并关闭工单通知，再单独启用社群。群组会话 ID 是成员群的数字 ID，可以与工单客服通知群不同。更换机器人令牌所对应的机器人后，需要重新保存并核验社群设置。

管理员配置步骤：

1. 将目标群设置为私密群，把机器人设为管理员，并授予「邀请用户」和「封禁用户」权限。
2. 在「客服与社群设置」填写群组 ID、显示名称和机器人用户名。启用时后端检查实际机器人、私密群及所需权限。
3. 注册或更新同一个回调地址，明确包含 `message`、`chat_join_request`、`chat_member` 三类更新。Telegram 默认更新集合不包含成员变动，不能省略 `chat_member`。
4. 撤销其他普通邀请链接，关闭公开入群入口，避免其他管理员手动批准没有本站个人邀请的用户。机器人会拒绝不符合条件的入群申请，并处理收到的新成员事件；Telegram 群主、管理员和启用前已在群里的成员仍需由群管理者自行核查。

用户操作流程：

1. 注册并登录网站，在「客服与社群」直接领取个人入群链接。此时只记录链接所属网站用户，不建立已入群绑定。
2. 使用链接在 Telegram 中申请入群，不需要先与机器人私聊，也不需要返回网站确认身份。
3. 机器人根据该邀请链接查找网站用户，校验账号有效状态，并原子记录本次申请使用的 Telegram 身份，避免同一邀请被多个人同时占用。
4. 审批并确认实际入群后，完成网站账号与 Telegram 账号绑定，将状态标记为「已入群」，同时安排撤销邀请。
5. 用户在网站查看实际绑定的 Telegram 身份和入群状态；退群后可用已绑定的身份重新领取邀请。

个人链接有效 15 分钟，同一用户的有效链接会复用。链接是限时、单次使用的入群凭证，请勿转发。首次申请前尚不知道领取者的 Telegram 身份，因此如果将链接转发给他人，机器人无法仅凭回调判断使用者是否为网站账号本人，可能把他人的 Telegram 绑定到该网站账号；限时和单次使用不能解决首次转发问题。系统保证只有登录且账号有效的注册用户能够领取本站邀请，一个 Telegram 身份只能归属一个网站账户，已完成的绑定不能被后来使用链接的人替换。

Telegram 回调提供申请者及所使用的邀请链接，不提供其网站登录身份，具体字段见 [Telegram 入群申请文档](https://core.telegram.org/bots/api#chatjoinrequest)。

社群回调先验证密钥并写入持久化任务，后台执行审批、成员状态同步及撤销链接，临时失败后重试。网站邀请码接口需要登录且禁止缓存；验证凭据不会写入公开页面。机器人无法在离线时即时管理群组，因此应保持网站服务运行，并维持上述群权限与回调配置。

成功处理回调后立即清空原始消息，只保留去重记录；后台按小时分批清理超过 7 天的已完成回调、过期超过 1 天的验证记录，以及撤销超过 30 天且未被会员授权引用的邀请。未完成任务、未撤销链接和账户绑定不会因该清理被删除。

接口与权限要求见 [Telegram 官方邀请接口](https://core.telegram.org/bots/api#createchatinvitelink)、[入群审批接口](https://core.telegram.org/bots/api#approvechatjoinrequest)和[成员更新说明](https://core.telegram.org/bots/api#update)。

## 设置充值用户专享的 VIP 群

在后台「客服与社群设置」(`/admin/community/settings`) 中操作：

1. 填写 VIP 群的群组 ID，将显示名称设置为「VIP 专享群」，启用社群。机器人、私密群权限和回调按上节配置。
2. 开启「设为 VIP 群，仅限曾成功支付余额充值的用户」。资格沿用批量邮件的充值口径：存在成功支付的余额充值订单，付款时间非空且实付金额大于 0。
3. 开启「登录后提示符合资格的用户加入 VIP 群」，保存设置。

这两个开关默认关闭，升级不会改变原来面向全部注册用户的普通群规则。当前配置管理一个群，开启充值限制即将该群设为 VIP 专享群。

用户曾成功充值即可取得资格，余额用完后仍保留资格。未支付订单、赠送余额、兑换码、返佣、管理员直接加余额及订阅购买都不算。沿用「曾成功支付」口径，后续退款不会抹除历史充值资格。

未充值用户不会看到 VIP 群卡片、邀请链接或加入弹窗，客服和工单入口仍正常显示。后端也会检查资格，直接调用接口或使用开启限制前领取的旧邀请不能绕过充值要求。入群审批和后台补偿处理会再次核查资格；数据库查询失败时保留重试，不当作无资格直接清退。

符合资格且尚未入群的用户登录或打开已登录的网站后，可看到加入提示；关闭后同一浏览器标签页会话内不反复弹出，重新登录后可再次提示。已入群用户不弹窗。弹窗不会自动生成邀请，用户点击后进入社群页面领取自己的链接，再按原流程入群绑定。网站公告和管理员确认弹窗优先展示。

个人链接仍是入群凭证，请勿转发；首次成功入群的 Telegram 身份会绑定到领取链接的网站账号。将已有普通群改为 VIP 群时，不会自动扫描并批量移除启用前的旧成员，管理员应自行核查存量成员；后台成员列表继续保留全部网站用户，方便查看实际入群情况。

该设置复用现有配置存储和支付订单用户索引，无需新增数据库迁移。

## 后台查看哪些用户进了群

管理员进入 `/admin/community/members` 的「社群成员」页面，查看网站用户、邮箱、账号状态、Telegram 身份和首次入群时间。可以搜索用户 ID、邮箱、网站用户名或 Telegram 数字 ID，并分页查看。

- 已入群：已经确认在当前配置群组中的网站用户。
- 未入群：所有当前未在群中的网站用户，包含从未领取邀请、待入群和已退群的用户。
- 待入群：已领取有效邀请或正在处理入群申请，但尚未确认入群。
- 已退群：已记录离开当前群组的用户。

列表从全部未删除的网站用户出发，未领取链接的人也会显示。顶部人数按当前搜索条件统计，切换状态筛选不会改变统计口径；领取链接或提交申请不会计入已入群人数。入群和退群事件由后台任务处理，状态会有短暂同步延迟。

## 图片与消息规则

- 支持 PNG、JPEG、GIF 和 WebP，单张最多 5 MiB，每条消息或批量邮件最多 4 张。
- 后端检查真实图片格式、完整可解码性和像素限制，不仅依赖扩展名。
- 可以发送文字、图片或图文组合。已关闭工单需重新打开后回复。
- 邮件中的图片使用内嵌附件，无需收件人登录网站才能查看。
- Telegram 通知中的图片以原图文件发送，保留清晰度并兼容长图、GIF 和 WebP。
- 每个账号最多保留 20 张未使用的草稿图片；移除图片会释放额度，超过 24 小时的草稿在下次上传时清理。批量邮件生成草稿后会保存独立图片快照并释放上传额度。
- 网页正文作为普通文本显示，邮件正文转义后生成 HTML，避免把工单内容作为脚本执行。

## 投递与故障排查

工单消息与通知任务在同一数据库事务中保存。后台任务使用租约领取通知及邮件收件人，重启后可以继续处理；失败按退避间隔重试。SMTP 和 Telegram 不提供跨系统事务，因此采用至少一次投递：若远端已成功接收、但本地在记录成功前中断，恢复后可能重复发送。Telegram 入站回调按更新编号去重，同一个更新不会重复写入工单。

通知功能未启用时，网页工单仍可使用。启用前请完成收件人及机器人配置。用户发言通知客服邮箱及机器人；客服回复也会邮件通知工单所属用户。

- 邮件失败：检查系统 SMTP 配置、收件地址、发件服务限流与垃圾邮件目录。
- 机器人收不到通知：检查令牌、会话 ID、机器人是否已加入会话以及服务器能否访问 `api.telegram.org`。
- 机器人回复未同步：检查回调 HTTPS 可达性、回调密钥、白名单、引用的消息是否由本站机器人发送，以及工单是否已关闭。
- 群组中使用匿名身份回复：改用白名单中指定的个人身份。
- 图片上传失败：检查格式、文件大小、像素尺寸及反向代理请求体限制；工单图片上传接口需允许略大于 5 MiB 的请求体。

## 本地验证

```powershell
# 在 frontend 目录运行
pnpm typecheck
pnpm build

# 在 backend 目录运行；Windows 默认 Go 为 32 位时显式选择 amd64
$env:GOARCH = 'amd64'
$env:CGO_ENABLED = '0'
go test ./internal/service ./internal/repository ./internal/handler -run 'Support|BulkEmail|EmailInline|Community' -count=1
go build -tags embed -ldflags='-s -w' ./cmd/server
```

真实投递验收需要自行配置测试 SMTP 收件箱和测试 Telegram 机器人：创建图文工单，确认网页、邮件和机器人中的图片；从 Telegram 引用回复图片，检查网页同步；最后使用仅包含测试账号的批量邮件草稿验证发送、进度和失败重试。

社群上线前请用测试群及两个测试身份验证：未登录不能领取邀请；登录后无需预先绑定即可领取；领取后仍未显示已入群；正常申请后自动绑定并显示已入群；已被认领的链接不能再由另一个身份使用；过期链接不能入群；退群后可重新领邀请；重复回调及重启不会生成重复绑定。后台还应确认从未领取邀请的用户出现在未入群列表中。

VIP 模式还应验证：只有赠送余额或未支付订单的用户看不到群入口和弹窗；成功余额充值后登录会提示；余额归零后资格仍在；已入群不弹；关闭弹窗后刷新不重复；退出再登录可以再次提示；普通群改为 VIP 后，无资格用户的旧邀请被拒绝。
